{
  "name": "kristina-math-tutor",
  "version": "1.0.0",
  "description": "Academic success dashboard for MAT 143 and ENG 111 using vanilla HTML, CSS, and JavaScript",
  "scripts": {
    "build:css": "tailwindcss -i ./src/styles/tailwind.css -o ./dist/output.css --minify",
    "watch:css": "tailwindcss -i ./src/styles/tailwind.css -o ./dist/output.css --watch",
    "dev": "npm run watch:css",
    "postinstall": "echo 'Dependencies installed. Run npm run build:css to compile Tailwind.'"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.1"
  }
}
